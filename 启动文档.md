# 本地部署启动文档

## 项目架构

```
┌─────────────────────────────────────────────┐
│           本地运行环境                        │
│                                             │
│  FastAPI Backend     :8000                  │
│  Streamlit Frontend  :8501                  │
│                                             │
│  Logs:   ./logs/                            │
│  ChromaDB: ./chroma_db/                     │
└──────┬──────────────────┬───────────────────┘
       │                  │
   端口 8000          端口 8501
   (后端 API)         (前端界面)
```

## 前提条件

- Python 3.12+
- 已配置 `.env` 文件（包含 SiliconFlow API Key）

## 1. 安装依赖

```bash
pip install -r requirements.txt
```

## 2. 配置环境变量

编辑根目录的 `.env` 文件，填入必需字段：

```env
# ===== LLM 对话模型（必填）=====
LLM_API_KEY=your_siliconflow_api_key
MODEL_NAME=deepseek-ai/DeepSeek-V4-Flash
MODEL_URL=https://api.siliconflow.cn/v1

# ===== Embedding 模型（RAG 功能必填）=====
EMBEDDING_API_KEY=your_siliconflow_api_key
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-8B
EMBEDDING_URL=https://api.siliconflow.cn/v1
```

> `EMBEDDING_API_KEY` 与 `LLM_API_KEY` 可使用同一个 SiliconFlow API Key。

## 3. 启动服务

### 启动后端（终端1）

```bash
python start_backend.py
```

### 启动前端（终端2）

```bash
python start_frontend.py
```

> **注意**：需要先启动后端，再启动前端。前端启动后会在浏览器自动打开。

## 4. 访问服务

| 服务 | 地址 |
|------|------|
| 前端界面 | http://localhost:8501 |
| 后端 API 文档（Swagger） | http://localhost:8000/docs |
| 健康检查 | http://localhost:8000/api/health |

## 常用运维命令

```bash
# 查看实时日志
# 后端日志输出在启动终端中，也可查看 logs/ 目录
# 前端日志输出在启动终端中，也可查看 logs/ 目录

# 停止服务
# 在对应终端按 Ctrl+C
```

## 数据持久化说明

| 路径 | 内容 |
|------|------|
| `./logs/` | 后端、前端运行日志 |
| `./chroma_db/` | RAG 向量库（重启不丢失） |

## 常见问题排查

| 现象 | 排查方式 |
|------|---------|
| 后端启动失败 | 检查 `.env` 中的 `LLM_API_KEY` 是否填写正确 |
| API 返回 500 错误 | 检查 API Key 是否有效，查看终端日志输出 |
| RAG 入库失败 | 检查 `EMBEDDING_API_KEY` 和 `EMBEDDING_MODEL` 配置 |
| 前端显示"后端服务未连接" | 确认后端已成功启动，`/api/health` 返回 200 |
| 端口被占用 | 修改 `.env` 中 `API_PORT` 或修改 `start_frontend.py` 中的端口号 |